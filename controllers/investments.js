const InvestmentModel = require('../models/Investment');

async function create(req, res){ 
    console.log(req.body);
    // try {
        
    // } catch(e) {

    // }
}

module.exports = {
    create, 
}